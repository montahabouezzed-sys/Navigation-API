# Navigation-API
A GNSS‑ or navigation‑related Python taking the user's  current GNSS coordinates, compares them to destination and computes distance, bearing, and trend (closer / farther / stationary) and returns a response
